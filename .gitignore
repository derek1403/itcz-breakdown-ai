# ============================================================
#  只保留 .py / .md / 圖片(pic)，其餘一律不上 git
#  （白名單法：先忽略全部，再逐一放行）
# ============================================================

# 1) 先忽略所有東西
*

# 2) 但保留資料夾結構，讓 git 能進入子目錄找白名單檔
!*/

# 3) 放行：程式碼與文件
!*.py
!*.md

# 4) 圖片：預設全部忽略（量太大）。要保留特定圖用：git add -f <path.png>
#!*.png
#!*.jpg
#!*.jpeg
#!*.gif
#!*.svg
#!*.webp
#!*.bmp

# 5) 放行：小型設定檔 (config.yaml / config_used.json 等)
!*.yaml
!*.yml
!*.json

# 6) 保留 .gitignore 本身
!.gitignore

# ------------------------------------------------------------
# 明確排除（即使上面放行也要擋掉的）
# ------------------------------------------------------------

# python 快取
__pycache__/

# 下載的論文 repo（自帶 .git，會變 embedded repo）+ 1.7GB 資料/h5
aux/investigation/ref/

paper/FTR/*

# ------------------------------------------------------------
# （可選）若也想保留設定檔，取消對應註解：
# !config.yaml
# !*.yaml
# !*.yml
# !*.json
# !*.txt
# ------------------------------------------------------------
